spring:
  datasource:
    url: jdbc:h2:mem:test
#    hikari:
#      schema: fbc-bot
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
    open-in-view: false
#  flyway:
#    schemas: fbc-bot